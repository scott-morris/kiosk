// Public.

const getNextEvent = (events) => {
  // Filter out events that have already started.
  // Sort events by time.
  // const orderedEvents = events.sort();
  // Return the first remaining event.
};

export default getNextEvent;
